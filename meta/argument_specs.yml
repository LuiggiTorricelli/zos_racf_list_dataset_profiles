---
argument_specs:
  main:
    short_description: "Main set of tasks of the role zos_racf_list_dataset_profiles"
    author: "Luiggi Torricelli"
    options:
      parameters:
        default: {}
        description: "An object containing parameters to be used on the RACF command"
        required: false
        type: "dict"
      show_output:
        default: true
        description: "A boolean to determine if output should be displayed or not at the end"
        required: false
        type: "bool"
